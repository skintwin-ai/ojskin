# MLflow / Decision Engine
# In production, you must configure MLflow (MLFLOW_TRACKING_URI + DECISION_MODEL_NAME/DECISION_MODEL_VERSION)
# or provide a local model path (DECISION_MODEL_PATH). Otherwise, DecisionEngine will raise at runtime.
MLFLOW_TRACKING_URI=
DECISION_MODEL_NAME=ojs_decision_model
DECISION_MODEL_VERSION=Production
DECISION_MODEL_PATH=
DECISION_AB_SPLIT=control:50,variant:50
DECISION_AB_STICKY_BY=submission_id

# Postgres / Redis
POSTGRESQL_URL=
POSTGRESQL_URLS=
POSTGRES_POOL_SIZE=5
POSTGRES_MAX_OVERFLOW=5
REDIS_HOST=localhost
REDIS_PORT=6379

# Environment
ENVIRONMENT=development

# Phase 1 production env additions
POSTGRESQL_URL=
POSTGRESQL_URLS=
POSTGRES_POOL_SIZE=5
POSTGRES_MAX_OVERFLOW=5

REDIS_HOST=localhost
REDIS_PORT=6379

ENABLE_ML_CONFLICT_RESOLUTION=false

DECISION_MODEL_NAME=ojs_decision_model
DECISION_MODEL_VERSION=
DECISION_MODEL_PATH=
MLFLOW_TRACKING_URI=
DECISION_AB_SPLIT=control:50,variant:50
DECISION_AB_STICKY_BY=submission_id
DECISION_AB_FORCE=
DECISION_THRESHOLD=0.5

EMAIL_PROVIDER=smtp
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_STARTTLS=false
SENDGRID_API_KEY=
SENDGRID_FROM=

SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
AWS_REGION=
TEMPLATE_DIR=
QUEUE_PROVIDER=redis
# SKZ Agents Framework - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/ojs_skz
REDIS_URL=redis://localhost:6379/0

# Security Configuration
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production
HMAC_SECRET=your-hmac-secret-key-for-api-security

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@domain.com
SMTP_PASSWORD=your-app-password

# OJS Integration
OJS_BASE_URL=http://localhost/ojs
OJS_API_KEY=your-ojs-api-key
OJS_USERNAME=admin
OJS_PASSWORD=admin

# Agent Configuration
AGENT_LOG_LEVEL=INFO
AGENT_MAX_WORKERS=4
AGENT_TIMEOUT=300

# Development/Production Mode
ENVIRONMENT=development
DEBUG=true

# File Storage
UPLOAD_PATH=./uploads
LOG_PATH=./logs

# API Configuration
API_HOST=localhost
API_PORT=8000
API_WORKERS=2
